package com.malt.task

/**
 * There may be only one owner concerned during the execution of this program: the user running it.
 */
object TaskOwnerIdHolder {

    lateinit var taskOwnerId: TaskOwnerId
}